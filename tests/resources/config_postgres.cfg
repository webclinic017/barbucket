[database]
dbms = postgresql
# sqlite, postgresql, etc.

username = username
# for database access, not used for sqlite

password = password
# for database access, not used for sqlite

url = 192.168.0.100
# loclahost, 192.168.0.100, etc., not used for sqlite

port = 5432
# not used for sqlite

database_name = barbucket
# not used for sqlite